# JWT 알고리즘 선택: HS384 vs RS256

## 결론

프로덕션 JWT 서명 알고리즘으로 **HS384(대칭키)**를 채택한다. RS256(비대칭키)은 채택하지 않는다.

## 1. 왜 HS384인가

### 1-1. 키 공유 인프라 부재 (RS256을 못 쓰는 근본 이유)

JWT 검증은 게이트웨이(`gateway-service`)에서 중앙집중적으로 이루어진다. 그런데 `common/JwtKeyConfig.rs256()`는 서비스가 뜰 때마다 **랜덤으로 새 RSA 키쌍을 생성**하는 구조라, 서비스마다(발급하는 user-service, 검증하는 gateway-service 등) 서로 다른 키를 갖게 된다. RS256을 실제로 쓰려면:

- 키쌍을 한 번만 생성
- 개인키는 발급 서비스(user-service)에만, 공개키는 검증하는 모든 서비스에 배포
- 배포 설정(`docker-compose.deploy.yml` 등)에 시크릿 관리 로직 추가

가 필요한데, 이 인프라가 아직 없다. 반면 HS384는 서비스 간에 이미 공유 중인 `jwt.secret` 값 하나로 서명·검증이 모두 되기 때문에 별도 인프라 없이 바로 동작한다.

### 1-2. 성능 — 실측 기반

**(a) 순수 알고리즘 벤치마크 (JMH, `common/src/jmh/.../JwtAlgorithmBenchmark.java`)**

BCrypt, DB, Redis, HTTP 등 다른 요소를 전부 제외하고 JWT 서명/검증 연산 자체만 측정:

| 연산 | HS384 | RS256 | 배율 |
|---|---:|---:|---|
| 서명(sign) | 98,387 ops/s | 454 ops/s | **HS384가 약 216배 빠름** |
| 검증(verify) | 53,003 ops/s | 9,909 ops/s | HS384가 약 5.3배 빠름 |

RSA 서명은 큰 수의 모듈러 거듭제곱 연산이라 원래 비싸고, HMAC(HS384)은 단순 해시 기반이라 훨씬 싸다. 이 차이는 BCrypt나 네트워크 지연과 무관하게, 순수 알고리즘 특성에서 나온다.

**(b) 부하테스트 (k6, VU 10/50/100/300, EC2 t3.large = 2 vCPU / 8GiB 기준 로컬 재현)**

| 지표 | HS384 | RS256 |
|---|---:|---:|
| login_duration (avg / p95) | 744ms / 1.63s | 1.14s / 3.51s |
| http_req_duration (avg) | 932ms | 1.49s |
| 완료된 로그인 반복 수 (동일 시간 내) | 988건 | 380건 (절반 이하) |
| VU 300 구간 호스트 CPU | ~85~98% | ~83~99% |

두 알고리즘 모두 VU 300에서 2 vCPU를 거의 포화시켰지만(즉 병목은 DB 커넥션 풀이 아니라 CPU), **RS256은 로그인 1건당 훨씬 많은 CPU 시간을 요구**해서 같은 CPU 포화 상태에서도 처리량이 절반 이하로 떨어졌다. 실제로 별도 실행에서는 RS256이 VU 300 진입 순간 순간적으로 커넥션을 거부(connection refused)하는 장애까지 관측됐다.

로그인 1건당 access token + refresh token 2개를 서명하므로, 이 알고리즘 비용 차이가 그대로 두 배로 누적된다.

## 2. 그러면 HS384는 안전한가 — 암호학적 강도와 진짜 신경 써야 할 지점

HS384(HMAC-SHA384)의 서명 키를 **순수 무차별대입(brute force)으로 알아내는 것은, 키가 충분히 길고 무작위라면 사실상 불가능한 시간이 든다.** 384비트 키 공간(2^384)은 지금 인류가 가진 모든 컴퓨팅 자원을 동원해도 우주의 나이보다 긴 시간이 걸리는 규모다. 즉 **알고리즘 자체의 암호학적 강도는 이미 충분하다** — RS256으로 바꾼다고 이 부분이 더 안전해지는 것도 아니다.

바꿔 말하면, HS384를 쓸 때 실제로 뚫릴 수 있는 지점은 "알고리즘이 약해서"가 아니라 **키를 어떻게 관리하느냐**다:

- 키가 충분히 무작위(CSPRNG로 생성)한가
- 키가 평문으로 코드/설정 파일에 커밋되어 있지 않은가
- 키에 접근 가능한 범위가 최소화되어 있는가 (필요한 서비스만)
- 키 유출 시 회전(rotation)이 가능한 구조인가

**현재 확인된 갭:** `user-service/src/main/resources/application.properties`의 `jwt.secret` 기본값(`dev-only-secret-key-please-change-in-real-deployment-32bytes+`)은 길이(61바이트)는 HS384 요구치(48바이트 이상)를 충족하지만, **사람이 읽을 수 있는 문구라 실제 엔트로피가 낮고, 평문으로 저장소에 커밋되어 있다.** 위에서 말한 "무차별대입으로 못 뚫는다"는 전제는 **키가 CSPRNG로 생성된 진짜 무작위 값일 때만** 성립하므로, 프로덕션에서는:

1. CSPRNG로 생성한 48바이트 이상의 랜덤 키로 교체
2. 평문 커밋을 지양하고 환경변수/시크릿 매니저 등으로 분리 저장
3. 유출 대비 키 회전 절차 마련

가 앞으로 신경 써야 할 실질적인 지점이다. 알고리즘 선택(HS384) 자체를 재검토할 필요는 없다.
