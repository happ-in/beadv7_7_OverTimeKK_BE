# 로그인 대기열(Admission Queue) 도입 근거

## 결론

로그인 API(`POST /api/users/login`) 앞단에 Redis 기반 대기열(`LoginQueueService`)을 두어, DB 커넥션 풀 크기(10개)보다 적은 인원(`login.queue.max-concurrent=8`)만 동시에 로그인 처리(DB 조회)를 하도록 제한한다. 정원을 넘는 요청은 실패시키지 않고 대기열에 세워 순서대로 승격시킨다.

이 문서는 실측 부하테스트 결과가 아니라, 코드/설정에서 확인 가능한 사실을 근거로 한 **이론적 판단**을 정리한 것이다. 실측 검증은 이미 준비된 `k6/reseat-scenarios/jwt-login-queue-load.js`와 Grafana `login-perf-challenge` 대시보드로 추후 진행한다.

## 1. 왜 필요한가 — 안 하면 생기는 문제

### 1-1. DB 커넥션 풀은 작고 고정된 자원이다

`user-service`는 `spring.datasource.hikari.*`를 별도로 지정하지 않아 **Spring Boot 기본값인 최대 10개** 커넥션으로 동작한다. 로그인 요청이 몰려 동시에 10개를 넘으면, 초과분은 즉시 실패하는 게 아니라 **HikariCP 내부에서 커넥션이 반납될 때까지 대기**한다. `connection-timeout`도 따로 지정하지 않아 기본값인 **30초**까지 대기한 뒤에야 `SQLTransientConnectionException`으로 실패한다.

### 1-2. 대기 중인 30초 동안, 톰캣 스레드가 함께 묶인다

커넥션을 기다리는 요청은 그 요청을 처리하던 **톰캣 워커 스레드를 점유한 채로 대기**한다. 이 스레드 풀은 로그인 API 전용이 아니라 `user-service`의 모든 엔드포인트(`/api/users/me`, `/api/users/{id}` 등)가 공유하는 자원이다. 따라서 로그인 요청 폭주가 스레드 고갈로 번지면, **로그인과 무관한 다른 API까지 함께 응답 불가 상태**가 될 수 있다.

### 1-3. 인접 서비스로 장애가 전파될 수 있다

`order-service`, `gateway-service` 등이 유저 조회/인증 검증을 위해 `user-service`를 호출하는 경로가 있다. `user-service`가 스레드 고갈로 느려지면, 이를 호출하는 쪽도 함께 타임아웃에 걸려 **장애가 옆 서비스로 전파**될 수 있다.

### 1-4. 이 프로젝트 특성상 실제로 발생 가능성이 높은 시나리오다

`performance-service`가 담당하는 티켓 예매 특성상, **공연 예매 오픈 직전에 로그인이 순간적으로 몰리는 상황**이 흔하게 발생한다. 이때 로그인 자체가 막히면 사용자는 표를 사기는커녕 로그인조차 못 하게 되어, 다른 어떤 장애보다 직접적인 비즈니스 영향을 준다.

### 1-5. 정황상 근거

이미 진행된 `JWT_ALGORITHM_DECISION.md`의 RS256 부하테스트(VU 300)에서 "connection refused"가 관측된 바 있다. 알고리즘 자체의 문제라기보다, 이 문서에서 설명하는 커넥션 풀/스레드 고갈 패턴이 그 상황에서 이미 한 차례 나타났을 가능성이 있다.

## 2. 왜 "풀 크기를 늘린다"가 아니라 "대기열로 앞단에서 막는다"인가

커넥션 풀을 단순히 늘리는 방식(예: 10 → 25)은 임시방편에 가깝다:

- DB 서버 자체의 `max_connections`은 여러 서비스가 나눠 쓰는 공유 자원이라 무한정 늘릴 수 없다.
- 풀을 키워도 "그 이상 몰리면 다시 같은 문제"라는 구조는 그대로 남는다 — 상한을 옮길 뿐 근본적으로 해결되지 않는다.

반면 Redis에서 동시 처리 인원을 세는 연산(`ZADD`/`ZCOUNT`)은 DB 커넥션을 전혀 소모하지 않고 마이크로초 단위로 끝난다. **"지금 몇 명이 DB를 두드리고 있는지"를 DB가 아니라 Redis에게 먼저 확인**하게 하면, 초과 요청을 DB 앞까지 보내지 않고 훨씬 싼 자원(Redis)에서 미리 걸러낼 수 있다. 실패 대신 대기(줄서기)로 바꾸는 것이라 사용자 경험 측면에서도 "에러"보다 "잠깐 기다림"이 낫다.

`max-concurrent=8`을 풀 크기(10)보다 살짝 낮게 잡은 것은, 로그인 이외의 DB 작업(헬스체크 등)이 쓸 여유분을 남겨두기 위함이다.

## 3. 알려진 한계 (실측 이전 시점의 설계상 트레이드오프)

- **`/login/queue/enter`가 인증 없이 호출 가능**: 로그인 이전 단계라 구조적으로 인증을 걸기 어렵다. 반복 호출로 큐 슬롯을 점유당하는 악용 가능성이 있으며, 필요시 게이트웨이 레벨 rate limit으로 보완해야 한다(별도 서비스 영역이라 이번 범위 밖).
- **정량적 검증 미완료**: 이 문서의 근거는 전부 설정값과 구조 분석에서 나온 이론적 추론이다. `k6/reseat-scenarios/jwt-login-queue-load.js` + Grafana `login-perf-challenge` 대시보드로 대기열 적용 전/후 실패율·지연시간·커넥션 풀 지표를 비교해 실측 검증하는 작업이 남아 있다.
